import 'package:flutter/material.dart';

class SubordinateScaffold extends StatelessWidget {
  const SubordinateScaffold({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Subordinate'));
  }
}
